{% extends '::bootstrap.html.twig' %}

{% block body -%}
    <h1>Editar Vlan</h1>

    {{ form_start(edit_form) }}
    <div class="form-group">
        {{ form_label(edit_form.descricao) }}
        {{ form_widget(edit_form.descricao, { 'attr': { 'class': 'form-control' } }) }}
    </div>
    <div class="form-group">
        {{ form_label(edit_form.endereco) }}
        {{ form_widget(edit_form.endereco, { 'attr': { 'class': 'form-control' } }) }}
    </div>    
    <div class="form-group">
        {{ form_label(edit_form.vlanRange) }}
        {{ form_widget(edit_form.vlanRange, { 'attr': { 'class': 'form-control' } }) }}
    </div>    
    <div class="form-group">
        {{ form_label(edit_form.portaSwitch) }}
        {{ form_widget(edit_form.portaSwitch, { 'attr': { 'class': 'form-control' } }) }}
    </div>    
     {{ form_widget(edit_form.submit, { 'attr': {'class': 'btn btn-sm btn-success' } }) }} <a href="{{ path('vlan') }}" class="btn btn-sm btn-info">Voltar para a Lista</a>
    
    {{ form_end(edit_form) }}
    
   {{ form_start(delete_form) }}
        {{ form_widget(delete_form.submit, {'attr': {'class': 'btn btn-sm btn-danger' } }) }} 
    {{ form_end(delete_form) }}  
{% endblock %}
