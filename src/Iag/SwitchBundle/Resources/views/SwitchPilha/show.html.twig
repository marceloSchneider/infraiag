{% extends '::bootstrap.html.twig' %}

{% block body -%}
    <h1>SwitchPilha</h1>

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Hostname</th>
                <td>{{ entity.hostname }}</td>
            </tr>
            <tr>
                <th>Numportas</th>
                <td>{{ entity.numPortas }}</td>
            </tr>
            <tr>
                <th>Marca</th>
                <td>{{ entity.marca }}</td>
            </tr>
            <tr>
                <th>Patrimonio</th>
                <td>{{ entity.patrimonio }}</td>
            </tr>
            <tr>
                <th>Pilhafisica</th>
                <td>{{ entity.pilhaFisica }}</td>
            </tr>
            <tr>
                <th>Posicaopilhalogica</th>
                <td>{{ entity.posicaoPilhaLogica }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('switchpilha') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('switchpilha_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
