{% extends '::bootstrap.html.twig' %}
{% block titulo %}
    Voice Panel
{% endblock %}
{% block body -%}
    

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Número de portas</th>
                <td>{{ entity.numPortas }}</td>
            </tr>
            <tr>
                <th>Número</th>
                <td>{{ entity.numero }}</td>
            </tr>
        </tbody>
    </table>
    {{ form_start(delete_form) }}
        <a href="{{ path('voicepanel_edit', { 'id': entity.id }) }}"class="btn btn-sm btn-primary">Editar</a> <a href="{{ path('voicepanel') }}" class="btn btn-sm btn-info">Voltar para a lista</a> {{ form_widget(delete_form.submit, {'attr': { 'class': 'btn btn-sm btn-danger' } } ) }}
    {{ form_end(delete_form) }}
{% endblock %}
