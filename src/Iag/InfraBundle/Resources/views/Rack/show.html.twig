{% extends '::bootstrap.html.twig' %}
{% block titulo %}
    Rack
{% endblock %}
{% block body -%}
    

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Identificação</th>
                <td>{{ entity.identificacao }}</td>
            </tr>
            <tr>
                <th>Quantidade de U's</th>
                <td>{{ entity.quantidadeU }}</td>
            </tr>
            <tr>
                <th>Sala</th>
                <td>{{ entity.sala }}</td>
            </tr>
        </tbody>
    </table>
    {{ form_start(delete_form) }}
        <a href="{{ path('rack_edit', { 'id': entity.id }) }}"class="btn btn-sm btn-primary">Editar</a> <a href="{{ path('rack_index') }}" class="btn btn-sm btn-info">Voltar para a lista</a> {{ form_widget(delete_form.submit, {'attr': { 'class': 'btn btn-sm btn-danger' } } ) }}
    {{ form_end(delete_form) }}
{% endblock %}
