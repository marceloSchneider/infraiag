{% extends '::bootstrap.html.twig' %}
{% block titulo %}
    Editar Patch Panel
{% endblock %}
{% block body -%}

   {{ form_start(edit_form) }}
    <div class="form-group">
        {{ form_label(edit_form.numPortas) }}
        {{ form_widget(edit_form.numPortas, { 'attr': { 'class': 'form-control' } }) }}
    </div>
    <div class="form-group">
        {{ form_label(edit_form.pilha) }}
        {{ form_widget(edit_form.pilha, { 'attr': { 'class': 'form-control' } }) }}
    </div>    
    <div class="form-group">
        {{ form_label(edit_form.rack) }}
        {{ form_widget(edit_form.rack, { 'attr': { 'class': 'form-control' } }) }}
    </div>
    
        {{ form_widget(edit_form.submit, { 'attr': {'class': 'btn btn-sm btn-success' } }) }} <a href="{{ path('patch_index') }}" class="btn btn-sm btn-info">Voltar para a Lista</a>
    
    {{ form_end(edit_form) }}
    
   {{ form_start(delete_form) }}
        {{ form_widget(delete_form.submit, {'attr': {'class': 'btn btn-sm btn-danger' } }) }} 
    {{ form_end(delete_form) }}  
{% endblock %}
