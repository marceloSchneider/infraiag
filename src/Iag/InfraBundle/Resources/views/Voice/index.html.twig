{% extends '::bootstrap.html.twig' %}

{% block body -%}
    <h1>Voice list</h1>

    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Par</th>
                <th>Ramal</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('voice_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.par }}</td>
                <td>{{ entity.ramal }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('voice_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('voice_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('voice_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
