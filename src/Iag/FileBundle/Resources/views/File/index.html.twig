{% extends '::bootstrap.html.twig' %}
{% block titulo %}
    Carregar lista
{% endblock %}
{% block body -%}
    <div class="hero-unit">
      <h1>Formato do arquivo</h1>
      <p>Para ser carregado o arquivo deve estar no formato csv e apresentar as seguintes colunas:</p>
      <p>
        <table class="table table-striped table-bordered table-hover container-fluid">
            <thead>
                <tr>
                    <th>Bloco</th>
                    <th>Andar</th>
                    <th>Sala</th>
                    <th>Patch Panel</th>
                    <th>Ponto</th>
                    <th>Status</th>
                    <th>Ramal</th>
                    <th>Voice Panel</th>
                    <th>Hostname</th>
                    <th>IP do Switch</th>
                    <th>Porta do Switch</th>
                    <th>Wireless</th>
                    <th>Camera</th>
                    <th>VLAN</th>
                    <th>Sala Rack</th>
                    <th>Id do Switch</th>
                </tr>
            </thead>
        </table>
      </p>
      <p>Apenas os dados são necessários. Os títulos das colunas devem ser retirados.</p>
    </div>
    {{ form_start(form) }}
        <div class="form-group">
            {{ form_label(form.arquivo) }}
            {{ form_widget(form.arquivo, { 'attr': { 'class': 'btn btn-sm btn-success' } }) }}
        </div>
        <div class="form-group">
            {{ form_widget(form.submit, { 'attr': { 'class': 'btn btn-primary', 'data-loading-text': 'Carregando...' } }) }}
        </div>
    {{ form_end(form) }}

    {{ form_start(updateForm) }}
        <div class="form-group">
            {{ form_widget(updateForm.submit, { 'attr': { 'class': 'btn btn-sm btn-success' } }) }}
        </div>
    {{ form_end(updateForm) }}
    
    

{% endblock %}